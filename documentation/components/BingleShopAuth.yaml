components:
  securitySchemes:
    BingleShopAuth:
      description: authentication for BingleShop RESTful API
      type: apiKey
      in: header
      name: X-API-apiKey
    bearerAuth:            
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
